1.账本管理
- 除系统配置,用户管理,流水类型设置类型映射外的其他所有功能 (账户 流水管理) 都在账本下隔离
2.账户管理
支持多账户,账户关联账号,用于统计用户数据
账户余额可手动维护,手动维护后自动计算(设置计算时间,从该时间后的流水计算余额),可以在任意时刻手动维护且可以手动设置开始计算时间(默认手动维护时间)
支持多币种
3.交易记录
- 收支类型: 收入/支出/转账
- 必填字段: 金额、账户、分类、日期
- 选填: 备注、标签、附件(发票图片)
- 有转账功能 (A账户转B账户)
- 同一账本下转账不计收支

3. 分类体系

- 支出分类: 餐饮/交通/购物/娱乐/居住/医疗/教育...   收入分类: 工资/奖金/投资/兑换...
- 一级分类通过分类映射在数据导入时映射到系统对应类型中
- 是否需要二级分类,二级分类以标签形式存在
  (如"餐饮"下有"早餐"/"午餐"/"晚餐")?

4. 报表统计
- 流水日历
- 月度收支报表 (支出 vs 收入)
- 分类占比饼图
- 趋势折线图 (每月支出对比)
- 时间范围筛选


1.账本与用户关系
一个用户可以拥有多个账本
且一个账本允许多个用户协作
用户创建账本后可以通过共享码分享,其他用户通过共享码加入账本
2."一级分类通过分类映射在数据导入时映射到系统对应类型"
    - 这个场景是什么?
    这个场景是用户微信或者支付宝导出的流水记录,这里通过映射将不同类型映射到系统对应类型上
账户关联账号指的是例如 银行卡是那个用户的,微信是那个用户的
有其他问题随时问我,先实现用户管理以及登录,菜单等功能,先将系统框架搭建起来再讨论具体实现

todo 首位用户(默认管理员)不可降级,管理用户时最少应该有一位管理员账户可用

 信用卡 金额最大为0,金额应该为负数
 账户可见性 - 可见性在账户上设置,设置为可见时账本内成员可以看到账户余额,不可见时只能看到该账户和相关流水无法看到余额,仅归属人可见,账户流水记录账本下成员皆可操作
 需要 status: active/archived
 余额调整记录调整记录,调整日期,余额可随时调整,调整后账户计算余额时以调整后余额和调整时间后的流水记录计算
 账户创建时设定的"初始余额"仅记录一个数值不生成流水



设置 - 单页面 - 手风琴或其他类似样式进行设置分组
- 通用
    - 是否开放注册(开放注册否以后登录页面将关闭注册入口只有登录入口,同时优化页面提示)
- 字典管理(同时其他用到字典列表的input改为 模糊搜索对应字典列表并可以直接输入新的item,并且会保存到字典列表中)
    - 账户类型
    - 开户行
有以下问题:
是否有其它配置项可以添加?


现在讨论流水管理实现方式
流水记录字段包含: 类型(收入 支出 转账(不计收支)) 收支类型 日期 金额 账户  归属人 交易方 备注 关联附件(小票\支付图片等)
列表分页查询 功能包含新增 更新 删除 复制 页面筛选 按筛选批量更新 列表直接编辑模式
汇总条件下记录的 总收入\支出\转账 净收入等汇总条件
完善账户余额计算逻辑


现在讨论一下流水日历模块
流水日历页面总体以每月日历为基础,
日历中每一天各自汇总当天收入和支出,点击后可以在当天添加流水,查看当日流水(弹窗且弹窗内可筛选),编辑流水
页面中汇总当月总支出 收入 盈余 转账等信息
还有什么可以优化的


需要为流水添加标签功能,标签数据来源为后续添加的自由预算,或者用户自由设置的文本
接下来讨论预算管理模块
预算分两种类型 一种是每月固定预算 比如房租 饮食 等,另外一种是自由预算 例如临时决定出去度假
(汇总流水标签含有该概算的流水,例如去三亚旅游的流水都打上三亚旅游的标签以汇总花费到预算中)
可以按月添加不同分类预算,然后在页面汇总当月总预算和当年总预算和分项预算
按类型汇总预算使用情况和总预算使用情况

用户添加预算时可以为不同月份批量添加预算(可以直接复制某个月份预算然后添加到对应其他月份中)(添加不同类型的预算后选择批量生成某几年某几个月的预算-可以自由选择月份)


1.固定收支添加标签字段,默认添加固定支出标签且不可删除(固定支出标签生成流水时会自动添加,是已有逻辑,检查下不要冲突),其他标签可自由添加
2.贷款预览中,待还款样式调整,当前样式会导致换行
3.固定收支列表表头和数据列对不上
4.固定收支列表状态按钮调整到操作中,且提示需要明显一些,可以和流水列表多选编辑后左侧色块高亮提示样式一致,颜色改成绿色
5.固定收支开关状态在编辑弹窗中提供开关选项

首页
当月预算使用情况
当月汇总,当月最大支出收入
当年汇总

现在讨论下 固定收支模块
固定收入\支出记录包含流水的一切字段,并添加定时任务字段(使用cron表达式存储但是页面配置时使用可视化编辑器编辑和反显)和固定收支类型(周期|贷款)
固定收支生成的流水记录打上 固定收支标签
该模块添加固定收支记录后会在指定时间生成一笔收入\支出记录
支持添加房贷车贷以及计算剩余金额和生成每月还款金额(标明本金和利息),利率以及计息方式可设定,贷款类型通过开始日期还款金额自动计算当前期数来生成流水

1.自由预算参数开始时间结束时间参数组件添加清除按钮
2.汇总逻辑查询和页面展示逻辑修改,将具体数据的汇总调整到列表页面,使用金额随列表返回前端,所有预算总额和使用总额在前端从列表中汇总

统计分析
账户变化
类型分析
归属人分析

1.图表可点击统计项目标题来展示或者隐藏该项
2.可双击或者其他交互形式查看图表区域统计数据明细
3.统计图表数据可通过筛选条件过滤,条件根据不同统计类型变动
4.前面图表基础上添加各账户日余额,月余额变化图表

图表页面改版
顶部历史总支出 总收入等不变
图表分为 首页 | 年度统计(默认当年) | 月度统计(默认当月) | 自由筛选(不自动查询数据,添加筛选条件点击搜索后再查询出图表,图表类型按月度统计设置)
首页: 月度收支趋势、资产净值趋势、账户余额变化(账本内全部账户)、雷达图│多维度财务健康评估
年度统计\月度统计:
年度下包含: 堆叠柱状图 │ 各月分类支出构成对比 , 当年的 雷达图│多维度财务健康评估
月度下包含: 堆叠柱状图 │ 各添分类支出构成对比 , 当月的 雷达图│多维度财务健康评估
年度月度公共包含
分 收入分析|支出分析|转账分析 三块
下方各包含 分类占比|归属占比|支付账户占比

图表有以下调整
1.年度月度统计布局调整,分类统计调整到上方
2.强化分类统计,放大图表大小,添加图例在下方,过多时换行展示
3.堆叠柱状图和分类饼图增强,单击后在下方展示当前图例名称和金额并展示查看详情按钮,点击后弹窗查看具体流水记录

年度月度统计中头部收入支出汇总两两换行展示在左侧,雷达图展示在右侧

弹窗删除日期参数,分页查询参数继承数据来源参数(类型|分类|日期范围将继承的查询参数显示在头部,并且不可更改),添加除继承参数外的其他参数查询

ai助手

可执行操作 & 主动建议

1.preview 模式下时需要用户点击 确认无误按钮                                  
后才继续返回数据调用llm,否则无限期等待                                          
2.卡片中的未匹配账户下数据未渲染                                                
3.preview模式下点击确认无误后(用户可能修改账户和分类映射规则),传输给llm数据时
应该按照最新规则匹配数据并传递给llm

这个流程不对,按以下流程改造
调用分析模式时,从数据库加载映射规则->将未映射的数据返回给llm
调用预览模式时,llm新补充映射规则和当前db映射规则合并进行映射,并展示
用户点击确认后返回映射后的流水数据给llm检查映射是否正确
正确后进入确认导入工具,确认导入工具展示要创建的账户和流水并可以指定归属人,点击保存时保存流水账户和映射规则
分析现有流程和以上流程差异并指定修改计划



ai添加图片记账和小票上传功能,调用ocr工具识别小票内容(ai识别)记账,或者通过ocr工具识别小票内容找到对应流水上传小票
    添加视觉模型OCR模型配置,工具内部传输图片给视觉模型模型返回金额\日期\交易方等字段,备注字段保存识别到的所有文本
    将工具返回结果提供给ai助手进行记账或者查找对应流水
    问题: 1.如果主力模型就是视觉模型是否可以直接传输图片给主模型而不用工具? 架构如何设计,现有框架是否有相关支持
         2.图片文件只给模型文件id,通过文件id进行关联上传和调用工具

[√] 编写dockerfile\docker-compose (需要注意puppeteer\当前项目是前后端分离\前后端访问\前后端端口) 问题-- 外部访问 当前服务分前\后端,当部署后需要外网访问时,是需要配置baseurl? 需要的话将其添加到环境变量中\端口可通过环境变量配置更改
[√] 多数据库支持,提供对sqlite\mysql\postgresql数据库的支持(可通过环境变量添加连接信息和使用数据库)
[√] readme文件编写,配置指导 sqlite最大数据量提示 默认提供sqlite,提示sqlite可用数据量
[√] readme文件
[√] github issus模板
[√] 数据迁出\迁入功能,当后期需要迁移db时需要将数据以及全部附件导出以及导入,在设置内添加该模块 提供全量数据导入导出以及附件导出(压缩包),导入功能,导出时提供数据选择,选择账本,选择模块,例如助手聊天记录可以选择不导出,(核心数据不能取消勾选,例如配置数据\账本\账户\流水\用户等模块)
[√] 设置中可以调整jwt过期时间 1天\7天\30天
[√] 添加ai审计页面,管理员可以查询查看用户ai操作,页面字段展示要人性化
[√] 移动端ui适配

## todo1
[√] app各服务器需要记录当前账本id,进入时打开该账本,而非第一个账本
[√] 服务器管理页面账号密码或者apikey必填(二选一),名称和地址必填
[√] 服务器管理页卡片添加复制按钮
[√] 记一笔弹窗tab添加记忆,关闭时是ai则打开也是ai
[√] 为app所有页面添加下拉刷新数据功能
[√] 记一笔弹窗两个tab高度相同,以高的为准
[√] 转账类型没有分类字段, 同时缺少归属人字段,字段逻辑和web端一致
[√] 将账本\账户\固定收支预算等页面的管理功能也实现到app中
[] app
[√] 记一笔ai助手聊天内部高度太高了,需要滚动才能看到底部输入框
[√] 流水日历的下拉属性改为日历区域下拉而非当日流水(会和流水列表冲突)
[√] 流水日历每个月的27(今天8/27)都被标记
[√] 删除移动端 ai审计功能
[√] app将两个ai聊天窗口合并,以ai财务助手的页面为主,会话列表以记一笔弹窗为准,同时删除咖啡等提示语句

[√] 联网搜索默认启用
[√] 流水缺少附件功能,需要图片预览和附件下载功能(ai聊天也一样需要预览图片),多附件操作逻辑参考web端
[√] 统计分析图表功能需要对标web端,如点击后下方出现分类总和并可以点击查看流水列表
[√] 统计分析图表需要对标web端
[√] 统计下的自由tab 日期输入框添加日期选择组件并且筛选条件需要同web端
[√] web端设置页面复刻添加到app中
[√] 流水日历整合节假日展示
[√] 现在ai聊天时发送附件有缺陷导致web端和移动端图片等附件不能同步,web端和移动端都有此问题,即聊天中发送给llm的图片虽然上传了但是聊天内容中没有保存也没有回显直接使用本地图片,后续本地缓存更新后图片或者附件会丢失(导入在聊天内容中只有文件id),这里回显应该读取已经上传的图片或者附件
[√] 移动端编辑流水后应该刷新列表(日历和流水管理),其他页面也应该是一样的逻辑(更新当前展示数据),请检查
[√] 有个问题,现在账户是在账本下的,而同一个账本下会有多个用户,每个用户都可以有一个微信或者支付宝账户,如何区分这些账户?,账户名称添加账号名?,现在代码里那些功能会因为该问题出错?
[√] 当用户上传多个待导入文件时,如上传一个后打断然后再上传一个,此时llm会同时调用两个确认导入工具,而导入工具没有做多工具控制(全部确认后再发给llm处理,现在已有该逻辑),会导致第二个工具点击后llm报错,将多工具处理推广到全部工具
[√] 移动端流水管理去重功能
[√] 移动端流水管理导入导出功能
[√] 导入预览列表添加筛选条件参考web端
[√] 导出字段调整,现在导出csv字段和系统字段有差异,同步系统字段和名称,另外导入时 配置列映射 - 其他CSV 系统字段列也调整同步到系统字段(删除系统不存在字段和调整已有字段名称)
[√] 移动端附件图标点击后可直接查看流水附件
[√] 移动端流水管理列表流水左滑菜单样式优化,菜单操作删除按钮添加确认弹窗(不要用默认弹窗),流水日历每日流水中也添加左滑菜单.同时删除菜单中的编辑按钮
[-] logo
[√] 渲染性能优化
[√] 样式优化,主题样式\动画优化,需要让页面体验更加丝滑,主题也需要更好的复刻web端主题(现在主题大部分只是换个颜色侧边栏颜色都没变,没有特色),用户登录后默认主题和主题选择同步web端
[√] 统计报表中可以点击查看轴上具体数据
[] 侧边菜单改为轮盘样式?
[√] 菜单权限管理
[] 移动端app配置服务器配置列表导入导出
[] 死代码删除,代码清理整理重构

[√] 移动端全局错误处理,抓取异常给提示toast或者弹窗提示重启重试(网络异常toast提示重试)

[] 移动端日志
[] 编写单元测试,web端,移动端,后端

## todo2
[] 模型配置可选是否为多模态模型,主模型选择是多模态模型时直接发送图片给主模型而不再调用ocr工具
[] 首页ai助手窗口改为右侧悬浮图标点击后打开对话窗口
[] 为助手提供图表绘制工具绘制图表给用户(打包echart?)
[] mcp服务

[√]
[-]
[x]


