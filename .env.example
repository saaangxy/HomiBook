# 复制为 .env 后按需修改，docker compose 会自动读取

# 外部访问前端入口端口（Nginx）
FRONTEND_PORT=8080

# 后端服务端口（容器内部 + 外部暴露）
BACKEND_PORT=3002
BACKEND_EXPOSE_PORT=3002

# JWT 密钥（生产环境务必修改为随机长字符串）
JWT_SECRET=change-me-to-a-long-random-string
